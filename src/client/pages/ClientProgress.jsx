export default function ClientProgress({ client, schema }) {
  return (
    <div className="myarc-animate-in">
      <h2 className="myarc-card-title">📈 Progress Tracker</h2>
      <p className="myarc-text-gray">Coming soon...</p>
    </div>
  )
}
